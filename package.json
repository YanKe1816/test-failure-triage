{
  "name": "test-failure-triage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "deploy": "wrangler deploy",
    "test:mcp:production": "node scripts/production-regression.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.118.0"
  }
}
